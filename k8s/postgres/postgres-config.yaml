# ConfigMap Kubernetes per configurare i parametri del database Postgres (nome DB e utente)
apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
data:
  POSTGRES_DB: hackhub_db
  POSTGRES_USER: admin
